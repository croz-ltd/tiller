import { ArgsTable, Stories } from "@storybook/addon-docs";

import { Dropdown } from "@tiller-ds/menu";
import { ThemeTokens } from "../utils";

# Dropdown

The `Dropdown` component is a button which triggers a dropdown menu when clicked on.

See the Simple story's code below to get a better understanding of how this component functions.

All props are described in detail on the [Dropdown Props](#dropdown-props) section of the documentation.

<Stories includePrimary={true} />

## Dropdown Props:

<ArgsTable of={Dropdown} />

## Dropdown Tokens:
<ThemeTokens component="Dropdown"/>
