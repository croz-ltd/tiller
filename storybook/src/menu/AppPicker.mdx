import { ArgsTable, Stories } from "@storybook/addon-docs";

import { AppPicker } from "@tiller-ds/menu";
import { ThemeTokens } from "../utils";

# App Picker

The `App Picker` component is a button which functions as a bridge between apps, traversing you from one to another
via the dropdown displayed when clicking on the button.

All props are described in detail on the [App Picker Props](#app-picker-props) section of the documentation.

<Stories includePrimary={true} />

## App Picker Props:

<ArgsTable of={AppPicker} />

## App Picker Tokens:
<ThemeTokens component="AppPicker"/>
