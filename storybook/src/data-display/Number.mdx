import { ArgsTable, Stories } from "@storybook/addon-docs";

import { Number } from "@tiller-ds/data-display";

# Number

Tiller Number Component uses `FormattedNumber` from `react-intl` library.

By default `<FormattedNumber>` will render the formatted number into a React.Fragment.

## **Prerequisites for functioning**:
 - Intl Provider (details [here](/docs/component-library-intl-intl--basic))

<Stories includePrimary={true} />

## Number Props:

<ArgsTable of={Number} />
