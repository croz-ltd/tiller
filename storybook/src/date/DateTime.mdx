import { ArgsTable, Stories } from "@storybook/addon-docs";

import { DateTime } from "@tiller-ds/date";

# Date Time

Tiller Date Time Component uses `FormattedDate` from `react-intl` library.

By default `<FormattedDate>` will render the formatted number into a React.Fragment.

<Stories includePrimary={true} />

## Date Time Props:

<ArgsTable of={DateTime} />
