import { ArgsTable, Stories } from "@storybook/addon-docs";
import { RichTextEditor } from "@tiller-ds/form-elements-advanced";

# Rich Text Editor

The `RichTextEditor` component is used to create and edit rich text content.
It is built on top of the Lexical Rich Text Editor.

<Stories includePrimary={true}/>

