import { ArgsTable, Story, Stories, Canvas } from "@storybook/addon-docs";

import { StatusButton } from "@tiller-ds/core";
import { ThemeTokens } from "../utils";

# Status Button

Status Button communicates the status of a Button.

There are four statuses available: `idle`, `success`, `waiting` and `error`.

The component inherits all props from the `Button` component, but contains an additional `status` prop.

<Stories includePrimary={true} />

## Status Button Props:

<ArgsTable of={StatusButton} include={["status", "children", "className", "custom", "hidden", "id", "leadingIcon", "menu", "rounded", "size", "trailingIcon", "variant", "tokens" ]} />

## Status Button Tokens:
<ThemeTokens component="StatusButton"/>
