import { ArgsTable, Story, Stories, Canvas } from "@storybook/addon-docs";

import { StatusButton } from "@tiller-ds/core";
import { ThemeTokens } from "../utils";

# Status Button

Status Button communicates the status of a Button.

There are four statuses available: `idle`, `success`, `waiting` and `error`.

Possible Status Button states are demonstrated below:

### Idle

<Canvas>
  <Story id="component-library-core-statusbutton--idle-filled" />
</Canvas>

### Success

<Canvas>
  <Story id="component-library-core-statusbutton--success-filled" />
</Canvas>

### Waiting

<Canvas>
  <Story id="component-library-core-statusbutton--waiting-filled" />
</Canvas>

### Error

<Canvas>
  <Story id="component-library-core-statusbutton--error-filled" />
</Canvas>

The component inherits all props from the `Button` component, but contains additional `status` prop.

<Stories includePrimary={true} />

## Status Button Props:

<ArgsTable of={StatusButton} include={["status", "children", "className", "custom", "hidden", "id", "leadingIcon", "menu", "rounded", "size", "trailingIcon", "variant", "tokens" ]} />

## Status Button Tokens:
<ThemeTokens component="StatusButton"/>
